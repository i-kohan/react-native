import { StyleSheet } from 'react-native'

export default styles = StyleSheet.create({
  content: {
    alignItems: 'center',
    justifyContent: 'center',
    height: '90%',
    width: '100%',
  }
})